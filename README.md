To run this project make sure you have Typescript installed and then:
* navigated to the root folder and run:
* * ```npm install```
* After the dependencies have finished installing run: 
* * ```npm run dev```
ctrl + click the green link in the terminal using localhost
* on port 3000 you can access the api docs using /docs